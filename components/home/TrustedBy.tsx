"use client";

/**
 * Trust logos now live inside the hero glass frame (under search).
 * Kept as a light spacer so sections below remain unchanged in order.
 */
export function TrustedBy() {
  return <div className="h-2 bg-snow" aria-hidden />;
}
